// Package repoconfig manages per-repository configuration stored inside
// .git/repoinjector/config.yaml. It tracks which global config items and
// directory entries are enabled for a particular repo, along with workflow
// state and merge URL metadata.
package repoconfig
